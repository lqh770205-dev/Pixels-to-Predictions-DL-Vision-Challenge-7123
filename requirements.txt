# Install PyTorch separately for your CUDA/CPU stack, e.g. from https://pytorch.org/get-started/locally/
# Then: pip install -r requirements.txt

peft>=0.13.0
transformers>=4.45.0
accelerate>=0.34.0
pillow>=10.0.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
